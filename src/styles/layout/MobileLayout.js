import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const MobileLayout = styled(Box)({
  padding: 16,
  margin: "0 auto",
  maxWidth: "412px",
  display: "flex",
  justifyContent: "center",
  flexDirection: "column",
  paddingBottom: "80px",
  gap: 8,
});
