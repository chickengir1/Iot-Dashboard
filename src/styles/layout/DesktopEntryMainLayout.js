import { Box, styled } from "@mui/material";

export const DesktopEntryMainLayout = styled(Box)({
  width: "500px",
  display: "flex",
  flexDirection: "column",
  maxHeight: "700px",
  minHeight: "700px",
  justifyContent: "space-between",
  gap: 12,
});
