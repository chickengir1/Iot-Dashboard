import { Box, styled } from "@mui/material";

export const MobileEntryLayout = styled(Box)({
  padding: 16,
  margin: "0 auto",
  maxWidth: "412px",
  display: "flex",
  flexDirection: "column",
  gap: 16,
});
