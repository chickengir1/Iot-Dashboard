/*
import { useEffect } from "react";
import { verifyToken } from "../error/authError";

export const useAuth = () => {
  useEffect(() => {
    try {
      const decodedToken = verifyToken();
      console.log(decodedToken);
    } catch (err) {
      throw new Error(err);
    }
  }, []);
};
*/

// 나중에 만들기
