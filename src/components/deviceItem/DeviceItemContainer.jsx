import DeviceItemUi from "./deviceItemUi";

const DeviceItemContainer = ({ name, description }) => {
  return <DeviceItemUi name={name} description={description} />;
};

export default DeviceItemContainer;
