/*
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

 if (typeof password !== 'string' || password.length < 7)

 if (typeof username !== "string" || username.trim().length < 2)

 const phoneRegex = /^\d{10,11}$/;
 백엔드에서 쓰이는 유효성 검사 정규식, 근데 폰 번호는 디비에 없어서 나중에 없애도 되고 이거 참고해서 만들면 됨
 */
